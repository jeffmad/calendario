(ns calendario.component.scheduler-test
  (:require [clojure.test :refer :all]
            [calendario.component.scheduler :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
