(ns calendario.component.hikaricp-test
  (:require [clojure.test :refer :all]
            [calendario.component.hikaricp :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1))))
